---
import Layout from '../layouts/Layout.astro';
---

<Layout
	title="TerminalSync - Your terminal, everywhere"
	description="One command transforms any terminal into a secure, globally accessible workspace — built for developers supervising AI coding agents."
>
	<!-- Navigation -->
	<nav class="nav">
		<div class="nav-container">
			<div class="nav-logo">
				<span class="logo-icon">▲</span>
				<span class="logo-text">TerminalSync</span>
			</div>
			<div class="nav-links">
				<a href="https://github.com/aleqsio/terminalsync" target="_blank" rel="noopener noreferrer" class="nav-link">
					<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
						<path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
					</svg>
					GitHub
				</a>
				<a href="#install" class="nav-cta">Install Now</a>
			</div>
		</div>
	</nav>

	<!-- Hero Section -->
	<section class="hero" id="hero" data-animate>
		<div class="container">
			<div class="hero-content">
				<h1 class="hero-title" data-animate>Your terminal, everywhere.</h1>
				<p class="hero-subtitle" data-animate>
					One command transforms any terminal into a secure, globally accessible workspace — built for developers supervising AI coding agents.
				</p>
				<div class="hero-cta" data-animate>
					<div class="install-box hero-install">
						<code id="hero-install-command">curl -fsSL https://aleqsio.com/install.sh | bash</code>
						<button class="copy-btn-large" data-copy="hero-install-command" aria-label="Copy install command">
							Copy
						</button>
					</div>
				</div>
				<div class="trust-badges" data-animate>
					<span class="badge">Open source</span>
					<span class="badge-separator">·</span>
					<span class="badge">Zero configuration</span>
					<span class="badge-separator">·</span>
					<span class="badge">No accounts required</span>
				</div>
			</div>

			<div class="video-placeholder" data-animate>
				<div class="video-container">
					<div class="video-inner">
						<div class="video-play-icon">▶</div>
						<p class="video-label">Explainer video coming soon</p>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Problem Section -->
	<section class="problem" id="problem" data-animate>
		<div class="container">
			<h2 class="section-title" data-animate>Claude Code doesn't wait for you to get home.</h2>
			<p class="section-body" data-animate>
				Your AI agent is refactoring authentication. You're in a meeting. By the time you're back, it's pushed breaking changes. The problem isn't AI capability — it's that terminals don't follow you. <strong>TerminalSync makes your terminal omnipresent.</strong>
			</p>

			<div class="features-grid" data-animate>
				<div class="feature-card" data-animate>
					<div class="feature-icon">✓</div>
					<h3 class="feature-title">Approve from anywhere</h3>
					<p class="feature-description">Review diffs, confirm deployments, inspect test output.</p>
				</div>

				<div class="feature-card" data-animate>
					<div class="feature-icon">⚡</div>
					<h3 class="feature-title">Real-time streaming</h3>
					<p class="feature-description">See agent output as it happens.</p>
				</div>

				<div class="feature-card" data-animate>
					<div class="feature-icon">🔄</div>
					<h3 class="feature-title">Session persistence</h3>
					<p class="feature-description">Reconnect instantly. Sessions survive disconnects.</p>
				</div>

				<div class="feature-card" data-animate>
					<div class="feature-icon">🖥️</div>
					<h3 class="feature-title">Any terminal, any shell</h3>
					<p class="feature-description">Works with bash, zsh, fish, PowerShell — whatever you use.</p>
				</div>

				<div class="feature-card" data-animate>
					<div class="feature-icon">🔒</div>
					<h3 class="feature-title">End-to-end encrypted</h3>
					<p class="feature-description">Cloudflare Tunnels. Zero open ports. Zero-trust network.</p>
				</div>

				<div class="feature-card" data-animate>
					<div class="feature-icon">👁️</div>
					<h3 class="feature-title">Open source</h3>
					<p class="feature-description">Full source available on GitHub.</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Setup Flow Section -->
	<section class="setup" id="setup" data-animate>
		<div class="container">
			<div class="section-header">
				<h2 class="section-title" data-animate>Setup takes less time than reading this.</h2>
				<p class="section-tagline" data-animate>No accounts. No configuration files. No port forwarding.</p>
			</div>

			<div class="steps" data-animate>
				<div class="step" data-animate>
					<div class="step-number">1</div>
					<h3 class="step-title">Install</h3>
					<p class="step-description">Run the curl installer. Adds terminalsync to PATH automatically.</p>
					<div class="code-block">
						<code id="install-command">curl -fsSL https://aleqsio.com/install.sh | bash</code>
						<button class="copy-btn" data-copy="install-command" aria-label="Copy install command">
							<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
								<path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
							</svg>
						</button>
					</div>
				</div>

				<div class="step" data-animate>
					<div class="step-number">2</div>
					<h3 class="step-title">Share</h3>
					<p class="step-description">Run <code class="inline-code">terminalsync share</code> in any terminal. A secure tunnel starts instantly.</p>
					<div class="code-block">
						<code>terminalsync share</code>
					</div>
				</div>

				<div class="step" data-animate>
					<div class="step-number">3</div>
					<h3 class="step-title">Connect</h3>
					<p class="step-description">Run <code class="inline-code">terminalsync connect</code> to get a QR code. Scan it — full terminal on your phone.</p>
					<div class="code-block">
						<code>terminalsync connect</code>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Final CTA Section -->
	<section class="final-cta" id="install" data-animate>
		<div class="container">
			<h2 class="cta-title" data-animate>Stop losing context when you leave your desk.</h2>
			<p class="cta-subtitle" data-animate>
				Install TerminalSync in 30 seconds. The next time your AI agent needs approval, you'll be ready.
			</p>
			<div class="cta-actions" data-animate>
				<div class="install-box">
					<code id="final-install-command">curl -fsSL https://aleqsio.com/install.sh | bash</code>
					<button class="copy-btn-large" data-copy="final-install-command" aria-label="Copy install command">
						Copy
					</button>
				</div>
			</div>
			<div class="cta-links" data-animate>
				<a href="https://github.com/aleqsio/terminalsync" target="_blank" rel="noopener noreferrer" class="text-link">
					View on GitHub →
				</a>
			</div>
		</div>
	</section>

	<!-- Footer -->
	<footer class="footer">
		<div class="container">
			<div class="footer-content">
				<p class="footer-text">Built by <a href="https://github.com/aleqsio" target="_blank" rel="noopener noreferrer">@aleqsio</a></p>
				<p class="footer-text">Open source under MIT License</p>
			</div>
		</div>
	</footer>
</Layout>

<script>
	// Check if user prefers reduced motion
	const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

	// Copy to clipboard functionality
	document.addEventListener('DOMContentLoaded', () => {
		const copyButtons = document.querySelectorAll('[data-copy]');

		copyButtons.forEach(button => {
			button.addEventListener('click', async () => {
				const targetId = button.getAttribute('data-copy');
				const targetElement = document.getElementById(targetId);

				if (targetElement) {
					const textToCopy = targetElement.textContent || '';

					try {
						await navigator.clipboard.writeText(textToCopy);

						// Visual feedback
						const originalHTML = button.innerHTML;
						if (button.classList.contains('copy-btn-large')) {
							button.textContent = 'Copied!';
						} else {
							button.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"></polyline></svg>';
						}
						button.classList.add('copied');

						// Flash the code block border
						const codeBlock = targetElement.closest('.code-block') || targetElement.closest('.install-box');
						if (codeBlock) {
							codeBlock.classList.add('flash-border');
							setTimeout(() => {
								codeBlock.classList.remove('flash-border');
							}, 600);
						}

						setTimeout(() => {
							button.innerHTML = originalHTML;
							button.classList.remove('copied');
						}, 2000);
					} catch (err) {
						console.error('Failed to copy:', err);
					}
				}
			});
		});
	});

	// Smooth scroll for navigation
	document.addEventListener('DOMContentLoaded', () => {
		const links = document.querySelectorAll('a[href^="#"]');

		links.forEach(link => {
			link.addEventListener('click', (e) => {
				const href = link.getAttribute('href');
				if (href && href !== '#') {
					e.preventDefault();
					const target = document.querySelector(href);
					if (target) {
						target.scrollIntoView({ behavior: 'smooth' });
					}
				}
			});
		});
	});

	// Nav scroll effect
	document.addEventListener('DOMContentLoaded', () => {
		const nav = document.querySelector('.nav');
		let lastScroll = 0;

		const handleScroll = () => {
			const currentScroll = window.pageYOffset;

			if (currentScroll > 100) {
				nav.classList.add('scrolled');
			} else {
				nav.classList.remove('scrolled');
			}

			lastScroll = currentScroll;
		};

		if (!prefersReducedMotion) {
			window.addEventListener('scroll', handleScroll, { passive: true });
		}
	});

	// Scroll-triggered fade-in animations
	document.addEventListener('DOMContentLoaded', () => {
		if (prefersReducedMotion) return;

		const observerOptions = {
			root: null,
			rootMargin: '0px 0px -40px 0px',
			threshold: 0.05
		};

		const observer = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				if (entry.isIntersecting) {
					entry.target.classList.add('animate-in');
					observer.unobserve(entry.target);
				}
			});
		}, observerOptions);

		// Observe all elements with data-animate
		const animateElements = document.querySelectorAll('[data-animate]');
		animateElements.forEach((el, index) => {
			// Add stagger delay to children within same parent
			const parent = el.parentElement;
			const siblings = parent ? Array.from(parent.querySelectorAll('[data-animate]')) : [];
			const siblingIndex = siblings.indexOf(el);

			if (siblingIndex > 0 && siblings.length > 1) {
				el.style.transitionDelay = `${siblingIndex * 0.05}s`;
			}

			observer.observe(el);
		});
	});

	// Animate step numbers counting up
	document.addEventListener('DOMContentLoaded', () => {
		if (prefersReducedMotion) return;

		const stepNumbers = document.querySelectorAll('.step-number');

		const animateCounter = (element) => {
			const target = parseInt(element.textContent || '0');
			const duration = 600;
			const steps = 20;
			const increment = target / steps;
			let current = 0;

			const timer = setInterval(() => {
				current += increment;
				if (current >= target) {
					element.textContent = target.toString();
					clearInterval(timer);
				} else {
					element.textContent = Math.floor(current).toString();
				}
			}, duration / steps);
		};

		const counterObserver = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				if (entry.isIntersecting) {
					animateCounter(entry.target);
					counterObserver.unobserve(entry.target);
				}
			});
		}, { threshold: 0.5 });

		stepNumbers.forEach(num => counterObserver.observe(num));
	});
</script>

<style>
	/* Container */
	.container {
		max-width: 1280px;
		margin: 0 auto;
		padding: 0 64px;
	}

	/* Navigation */
	.nav {
		position: sticky;
		top: 0;
		z-index: 1000;
		background: rgba(10, 10, 10, 0.8);
		backdrop-filter: blur(12px);
		border-bottom: 1px solid var(--border);
	}

	.nav-container {
		max-width: 1280px;
		margin: 0 auto;
		padding: 20px 64px;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.nav-logo {
		display: flex;
		align-items: center;
		gap: 12px;
		font-weight: 800;
		font-size: 18px;
	}

	.logo-icon {
		color: var(--accent);
		font-size: 20px;
	}

	.nav-links {
		display: flex;
		align-items: center;
		gap: 24px;
	}

	.nav-link {
		color: var(--text-secondary);
		text-decoration: none;
		font-size: 15px;
		font-weight: 500;
		display: flex;
		align-items: center;
		gap: 8px;
		transition: color 0.2s ease;
	}

	.nav-link:hover {
		color: var(--text-primary);
	}

	.nav-cta {
		background: var(--accent);
		color: var(--bg);
		padding: 10px 20px;
		border-radius: 8px;
		text-decoration: none;
		font-weight: 600;
		font-size: 14px;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}

	.nav-cta:hover {
		transform: translateY(-2px);
		box-shadow: 0 0 16px rgba(0, 245, 160, 0.4);
	}

	/* Hero Section */
	.hero {
		padding: 128px 0;
		position: relative;
	}

	.hero-content {
		text-align: center;
		max-width: 900px;
		margin: 0 auto 80px;
	}

	.hero-title {
		font-size: 64px;
		font-weight: 900;
		line-height: 1.1;
		margin-bottom: 24px;
		letter-spacing: -0.02em;
	}

	.hero-subtitle {
		font-size: 20px;
		color: var(--text-secondary);
		max-width: 700px;
		margin: 0 auto 40px;
		line-height: 1.6;
	}

	.hero-cta {
		margin-bottom: 32px;
	}

	.hero-install {
		max-width: 600px;
		margin: 0 auto;
	}

	.btn-primary {
		display: inline-block;
		background: linear-gradient(135deg, var(--accent) 0%, var(--accent-dark) 100%);
		color: var(--bg);
		padding: 16px 40px;
		border-radius: 12px;
		text-decoration: none;
		font-weight: 700;
		font-size: 16px;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
		box-shadow: 0 0 24px rgba(0, 245, 160, 0.3);
	}

	.btn-primary:hover {
		transform: scale(1.05);
		box-shadow: 0 0 32px rgba(0, 245, 160, 0.5);
	}

	.trust-badges {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 12px;
		font-size: 14px;
		color: var(--text-muted);
	}

	.badge {
		color: var(--text-secondary);
	}

	.badge-separator {
		color: var(--border-hover);
	}

	/* Video Placeholder */
	.video-placeholder {
		max-width: 800px;
		margin: 0 auto;
	}

	.video-container {
		background: var(--surface);
		border: 1px solid var(--border);
		border-radius: 12px;
		overflow: hidden;
		aspect-ratio: 16 / 9;
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
	}

	.video-inner {
		text-align: center;
	}

	.video-play-icon {
		width: 72px;
		height: 72px;
		border-radius: 50%;
		background: rgba(0, 245, 160, 0.1);
		border: 2px solid rgba(0, 245, 160, 0.3);
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 28px;
		color: var(--accent);
		margin: 0 auto 16px;
		transition: all 0.3s ease;
	}

	.video-container:hover .video-play-icon {
		background: rgba(0, 245, 160, 0.15);
		border-color: var(--accent);
		box-shadow: 0 0 24px rgba(0, 245, 160, 0.2);
		transform: scale(1.05);
	}

	.video-label {
		color: var(--text-muted);
		font-size: 14px;
	}

	/* Problem Section */
	.problem {
		padding: 128px 0;
		text-align: center;
		background: var(--surface);
	}

	.section-title {
		font-size: 48px;
		font-weight: 900;
		line-height: 1.2;
		margin-bottom: 24px;
		letter-spacing: -0.02em;
		max-width: 900px;
		margin-left: auto;
		margin-right: auto;
	}

	.section-body {
		font-size: 20px;
		color: var(--text-secondary);
		max-width: 800px;
		margin: 0 auto;
		line-height: 1.7;
	}

	.section-body strong {
		color: var(--text-primary);
		font-weight: 600;
	}

	/* Setup Section */
	.setup {
		padding: 128px 0;
	}

	.section-header {
		text-align: center;
		margin-bottom: 80px;
	}

	.section-tagline {
		font-size: 18px;
		color: var(--text-secondary);
		margin-top: 16px;
	}

	.steps {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: 48px;
	}

	.step {
		text-align: center;
	}

	.step-number {
		width: 56px;
		height: 56px;
		background: var(--surface);
		border: 2px solid var(--border);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 24px;
		font-weight: 800;
		color: var(--accent);
		margin: 0 auto 24px;
	}

	.step-title {
		font-size: 24px;
		font-weight: 800;
		margin-bottom: 12px;
	}

	.step-description {
		color: var(--text-secondary);
		margin-bottom: 24px;
		line-height: 1.6;
	}

	.inline-code {
		background: var(--code-bg);
		padding: 2px 8px;
		border-radius: 4px;
		font-family: var(--font-mono);
		font-size: 14px;
		color: var(--accent);
	}

	/* Code Block */
	.code-block {
		background: var(--code-bg);
		border: 1px solid var(--border-hover);
		border-radius: 8px;
		padding: 16px;
		font-family: var(--font-mono);
		font-size: 14px;
		color: var(--accent);
		text-align: left;
		position: relative;
		overflow-x: auto;
	}

	.code-block code {
		display: block;
		white-space: nowrap;
	}

	.copy-btn {
		position: absolute;
		top: 12px;
		right: 12px;
		background: var(--surface);
		border: 1px solid var(--border);
		border-radius: 6px;
		padding: 8px;
		color: var(--text-secondary);
		cursor: pointer;
		transition: all 0.2s ease;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.copy-btn:hover {
		background: var(--surface-hover);
		color: var(--text-primary);
		border-color: var(--border-hover);
	}

	.copy-btn.copied {
		color: var(--accent);
		border-color: var(--accent);
	}

	/* Phone Mockup */
	.phone-mockup {
		margin-top: 24px;
		display: flex;
		justify-content: center;
	}

	.phone-screen {
		width: 200px;
		height: 120px;
		background: var(--code-bg);
		border: 2px solid var(--border);
		border-radius: 12px;
		padding: 12px;
		overflow: hidden;
	}

	.phone-terminal {
		font-family: var(--font-mono);
		font-size: 10px;
		line-height: 1.6;
	}

	.phone-line {
		margin-bottom: 2px;
	}

	.prompt-small {
		color: var(--accent);
		margin-right: 4px;
	}

	.output-small {
		padding-left: 12px;
		color: var(--text-muted);
	}

	.success-small {
		color: var(--accent);
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 32px;
		margin-top: 64px;
	}

	.feature-card {
		background: var(--bg);
		border: 1px solid var(--border);
		border-radius: 16px;
		padding: 40px;
		transition: all 0.3s ease;
	}

	.feature-card:hover {
		border-color: rgba(0, 245, 160, 0.3);
		transform: translateY(-4px);
		box-shadow: 0 8px 24px rgba(0, 245, 160, 0.1);
	}

	.feature-icon {
		font-size: 32px;
		margin-bottom: 16px;
	}

	.feature-title {
		font-size: 20px;
		font-weight: 800;
		margin-bottom: 12px;
	}

	.feature-description {
		color: var(--text-secondary);
		line-height: 1.6;
	}



	/* Final CTA Section */
	.final-cta {
		padding: 128px 0;
		text-align: center;
	}

	.cta-title {
		font-size: 48px;
		font-weight: 900;
		line-height: 1.2;
		margin-bottom: 24px;
		letter-spacing: -0.02em;
		max-width: 900px;
		margin-left: auto;
		margin-right: auto;
	}

	.cta-subtitle {
		font-size: 20px;
		color: var(--text-secondary);
		max-width: 700px;
		margin: 0 auto 48px;
		line-height: 1.6;
	}

	.cta-actions {
		margin-bottom: 32px;
	}

	.install-box {
		max-width: 600px;
		margin: 0 auto;
		background: var(--code-bg);
		border: 1px solid var(--border-hover);
		border-radius: 12px;
		padding: 20px 24px;
		display: flex;
		align-items: center;
		gap: 16px;
	}

	.install-box code {
		flex: 1;
		font-family: var(--font-mono);
		font-size: 14px;
		color: var(--accent);
		overflow-x: auto;
		white-space: nowrap;
	}

	.copy-btn-large {
		background: var(--accent);
		color: var(--bg);
		border: none;
		border-radius: 8px;
		padding: 12px 24px;
		font-weight: 600;
		font-size: 14px;
		cursor: pointer;
		transition: all 0.2s ease;
		white-space: nowrap;
	}

	.copy-btn-large:hover {
		transform: scale(1.05);
		box-shadow: 0 0 16px rgba(0, 245, 160, 0.4);
	}

	.copy-btn-large.copied {
		background: var(--surface);
		color: var(--accent);
		border: 1px solid var(--accent);
	}

	.cta-links {
		margin-top: 32px;
	}

	.text-link {
		color: var(--text-secondary);
		text-decoration: none;
		font-size: 16px;
		font-weight: 500;
		transition: color 0.2s ease;
	}

	.text-link:hover {
		color: var(--accent);
	}

	/* Footer */
	.footer {
		padding: 64px 0;
		border-top: 1px solid var(--border);
	}

	.footer-content {
		text-align: center;
		display: flex;
		flex-direction: column;
		gap: 8px;
	}

	.footer-text {
		color: var(--text-muted);
		font-size: 14px;
	}

	.footer-text a {
		color: var(--text-secondary);
		text-decoration: none;
		transition: color 0.2s ease;
	}

	.footer-text a:hover {
		color: var(--accent);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.container {
			padding: 0 32px;
		}

		.nav-container {
			padding: 16px 32px;
		}

		.nav-logo {
			font-size: 16px;
		}

		.logo-icon {
			font-size: 18px;
		}

		.nav-links {
			gap: 16px;
		}

		.nav-link {
			font-size: 14px;
		}

		.nav-link svg {
			display: none;
		}

		.hero {
			padding: 80px 0;
		}

		.hero-title {
			font-size: 40px;
		}

		.hero-subtitle {
			font-size: 18px;
		}

		.section-title {
			font-size: 32px;
		}

		.section-body {
			font-size: 18px;
		}

		.setup,
		.final-cta,
		.problem {
			padding: 80px 0;
		}

		.steps {
			grid-template-columns: 1fr;
		}

		.features-grid {
			grid-template-columns: 1fr;
		}

		.cta-title {
			font-size: 32px;
		}

		.cta-subtitle {
			font-size: 18px;
		}

		.install-box {
			flex-direction: column;
			gap: 12px;
		}

		.install-box code {
			text-align: center;
		}

		.video-container {
			aspect-ratio: 16 / 9;
		}
	}

	/* ============================================
	   MICRO-INTERACTIONS & ANIMATIONS
	   ============================================ */

	/* Scroll-triggered fade-in animations */
	[data-animate] {
		opacity: 0;
		transform: translateY(16px);
		transition: opacity 0.35s ease-out, transform 0.35s ease-out;
	}

	[data-animate].animate-in {
		opacity: 1;
		transform: translateY(0);
	}

	/* Nav scroll effect */
	.nav {
		transition: background-color 0.3s ease, border-color 0.3s ease;
	}

	.nav.scrolled {
		background: rgba(10, 10, 10, 0.95);
		border-bottom-color: var(--border-hover);
	}

	/* Enhanced button hover effects */
	.btn-primary {
		position: relative;
		will-change: transform;
		transition: transform 0.2s ease-out, box-shadow 0.2s ease-out;
	}

	.btn-primary:hover {
		transform: scale(1.05);
		box-shadow: 0 0 40px rgba(0, 245, 160, 0.6);
	}

	.btn-primary:active {
		transform: scale(0.98);
	}

	.nav-cta {
		will-change: transform;
		transition: transform 0.2s ease-out, box-shadow 0.2s ease-out;
	}

	.nav-cta:hover {
		transform: translateY(-2px);
		box-shadow: 0 0 20px rgba(0, 245, 160, 0.5);
	}

	.nav-cta:active {
		transform: translateY(0);
	}

	/* Copy button enhanced feedback */
	.copy-btn {
		will-change: transform;
		transition: all 0.2s ease-out;
	}

	.copy-btn:active {
		transform: scale(0.95);
	}

	.copy-btn.copied {
		animation: copy-pulse 0.4s ease-out;
	}

	@keyframes copy-pulse {
		0%, 100% { transform: scale(1); }
		50% { transform: scale(1.1); }
	}

	.copy-btn-large {
		will-change: transform;
		transition: all 0.2s ease-out;
	}

	.copy-btn-large:active {
		transform: scale(0.98);
	}

	.copy-btn-large.copied {
		animation: copy-pulse 0.4s ease-out;
	}

	/* Code block flash border on copy */
	.code-block,
	.install-box {
		transition: border-color 0.3s ease;
	}

	.code-block.flash-border,
	.install-box.flash-border {
		animation: flash-border-animation 0.6s ease-out;
	}

	@keyframes flash-border-animation {
		0%, 100% { border-color: var(--border-hover); }
		50% {
			border-color: var(--accent);
			box-shadow: 0 0 12px rgba(0, 245, 160, 0.3);
		}
	}

	/* Enhanced card hover with gradient overlay */
	.feature-card {
		position: relative;
		overflow: hidden;
	}

	.feature-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(135deg, rgba(0, 245, 160, 0.03) 0%, transparent 50%);
		opacity: 0;
		transition: opacity 0.3s ease;
		pointer-events: none;
	}

	.feature-card:hover::before {
		opacity: 1;
	}

	/* Hero background glow */
	.hero {
		position: relative;
		overflow: hidden;
	}

	.hero::before {
		content: '';
		position: absolute;
		top: 0;
		left: 50%;
		transform: translateX(-50%);
		width: 800px;
		height: 800px;
		background: radial-gradient(circle, rgba(0, 245, 160, 0.08) 0%, transparent 70%);
		pointer-events: none;
		z-index: 0;
	}

	.hero .container {
		position: relative;
		z-index: 1;
	}

	/* Video placeholder glow */
	.video-container {
		animation: video-glow 3s ease-in-out infinite;
	}

	@keyframes video-glow {
		0%, 100% {
			box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4), 0 0 0 rgba(0, 245, 160, 0);
		}
		50% {
			box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4), 0 0 30px rgba(0, 245, 160, 0.15);
		}
	}

	/* Section separators with subtle glow */
	.problem,
	.setup,
	.final-cta {
		position: relative;
	}

	.problem::before,
	.setup::before,
	.final-cta::before {
		content: '';
		position: absolute;
		top: 0;
		left: 50%;
		transform: translateX(-50%);
		width: 80%;
		max-width: 800px;
		height: 1px;
		background: linear-gradient(90deg, transparent, var(--border-hover) 20%, var(--border-hover) 80%, transparent);
		opacity: 0.5;
	}

	/* Step number animation */
	.step-number {
		transition: all 0.3s ease;
	}

	.step:hover .step-number {
		transform: scale(1.1);
		border-color: var(--accent);
		box-shadow: 0 0 20px rgba(0, 245, 160, 0.2);
	}

	/* Disable animations for users who prefer reduced motion */
	@media (prefers-reduced-motion: reduce) {
		*,
		*::before,
		*::after {
			animation-duration: 0.01ms !important;
			animation-iteration-count: 1 !important;
			transition-duration: 0.01ms !important;
		}

		[data-animate] {
			opacity: 1;
			transform: none;
		}
	}
</style>
